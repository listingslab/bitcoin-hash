{
  "scripts": {
    "test": "clear && cd react-app && npm test",
    "start": "clear && cd react-app && npm start",
    "build": "clear && cd react-app && npm run build",
    "deploy": "clear && firebase deploy"
  }
}
